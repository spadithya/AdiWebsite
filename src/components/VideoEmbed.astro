---
interface Props {
  id: string;
  title?: string;
  caption?: string;
  vertical?: boolean;
}

const { id, title = 'Video', caption, vertical = false } = Astro.props;
---

<figure class={`video-embed ${vertical ? 'vertical' : ''}`}>
  <div class="video-frame">
    <iframe
      src={`https://www.youtube-nocookie.com/embed/${id}?rel=0&modestbranding=1`}
      title={title}
      frameborder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
      referrerpolicy="strict-origin-when-cross-origin"
      allowfullscreen
      loading="lazy"></iframe>
  </div>
  {caption && <figcaption>{caption}</figcaption>}
</figure>

<style>
  .video-embed {
    margin: 2.5rem 0;
  }
  .video-frame {
    position: relative;
    padding-bottom: 56.25%; /* 16:9 default */
    height: 0;
    overflow: hidden;
    background: var(--bg-card);
    border: 1px solid var(--line-glow);
    box-shadow: 0 0 24px rgba(91, 255, 133, 0.08);
  }
  /* Vertical / Shorts: 9:16, constrained max width to keep it phone-sized */
  .video-embed.vertical {
    max-width: 360px;
    margin-left: auto;
    margin-right: auto;
  }
  .video-embed.vertical .video-frame {
    padding-bottom: 177.78%; /* 9:16 */
  }
  .video-frame iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }
  figcaption {
    margin-top: 0.8rem;
    font-family: var(--font-mono);
    font-size: 12px;
    color: var(--muted);
    letter-spacing: 0.04em;
    text-align: center;
  }
</style>
